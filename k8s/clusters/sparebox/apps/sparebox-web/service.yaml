apiVersion: v1
kind: Service
metadata:
  name: sparebox-web
  namespace: gitops-test
spec:
  selector:
    app: sparebox-web
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
