{ pkgs, ... }:
{
  # Phase 2 (weeks 7-14) — not started. Placeholder so the flake evaluates
  # cleanly today; fill in once Week 7 begins.

  # services.k3s = {
  #   enable = true;
  #   role = "server";
  # };
}
